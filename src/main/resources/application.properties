spring.application.name=backend_central

# Datasource (Supabase PostgreSQL)
spring.datasource.url=jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.urxikcewnimujwoanupx
spring.datasource.password=50563124asas@
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.preferQueryMode=simple

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=${PORT:8080}

# JWT
jwt.secret=${JWT_SECRET:xK9mP2nQ8rT5vW1yZ4aB7cE0hJ3lN6pR9sU2wX5yA8dG1kM4oQ7tV0bF3iL6nP}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# App config
app.b1-issuer=${B1_ISSUER:billing.dominio.com}
app.b2-token-expiration-days=${B2_TOKEN_EXPIRATION_DAYS:365}
app.b2-renew-url=${B2_RENEW_URL:http://localhost:8081/renovar}
app.frontend-url=${FRONTEND_URL:http://localhost:3000}
app.trial-days=${TRIAL_DAYS:30}
app.grace-days=${GRACE_DAYS:7}

# Logging
logging.level.com.membresia=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=INFO

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.pool-name=HikariPool-Membresias

# Default admin seeded on first startup
admin.default.nombre=Admin
admin.default.email=admin@backend.com
admin.default.password=admin123

## CORS Especificar dominios permitidos
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=false
spring.web.cors.max-age=3600